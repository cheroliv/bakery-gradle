[versions]
bakery = "0.1.2"
readme = "0.0.0"

[libraries]

[plugins]
bakery = { id = "com.cheroliv.bakery", version.ref = "bakery" }
readme = { id = "com.cheroliv.readme", version.ref = "readme" }

[bundles]
