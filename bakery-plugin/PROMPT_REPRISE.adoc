= Prompt de Reprise — Session 6

____
**Mission** : EPIC 6 — Integration tache publishProfil dans le plugin bakery
**Statut** : Session 5 OK — Integration publishProfile + version 0.1.0 publiee
____

'''

== Session 5 — Resume

**Date** : 2026-04-25
**Resultat** : OK — PublishProfileTask implementee, 2/2 Cucumber, version 0.1.0 publiee sur Gradle Plugin Portal

**Archive** : `.agents/sessions/3-integration-publish-profile.adoc`

'''

== Session 6 — Priorites

_Mission_ : Finaliser la migration publishProfile cote projets consommateurs.

. **Mettre a jour `cheroliv.com`** : Incrementer version plugin a 0.1.0 dans `gradle/libs.versions.toml`, ajouter section `pushProfile` + `profileFiles` dans `site.yml`, nettoyer le build script inline (retirer data classes, `profileCredentials`, tache `publishProfil`)
. **Verifier** : `./gradlew publishProfile` fonctionne avec la nouvelle config `site.yml`
. **Nettoyage build script** : Supprimer le bloc `buildscript` (dependances jackson/jgit devenues inutiles), les data classes `ProfileSiteConf`/`ProfilePushConf`/`ProfileRepoConf`/`ProfileCreds`, la propriete `profileCredentials`, et la tache inline `publishProfil`

=== Criteres d'Acceptation

* [ ] Version plugin 0.1.0 dans `cheroliv.com/gradle/libs.versions.toml`
* [ ] Section `pushProfile` + `profileFiles` presents dans `cheroliv.com/site.yml`
* [ ] `./gradlew publishProfile` pousse README.md et README_fr.md vers cheroliv/cheroliv
* [ ] Code inline supprime de `cheroliv.com/build.gradle.kts` (data classes, profileCredentials, tache publishProfil)
* [ ] Bloc `buildscript` supprime de `cheroliv.com/build.gradle.kts`
* [ ] `./gradlew serve` fonctionne toujours

'''

== Regles

* Regle 0 : `publishToMavenLocal` apres chaque modif code plugin
* Jamais de commit sans permission
* Regle de surete : verifier .gitignore avant de modifier un fichier de config
* Racine de travail = `bakery-plugin/`
* Archive + PROMPT_REPRISE mis a jour en fin de session